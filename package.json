{
  "name": "irsanai-live-share",
  "version": "0.5.3",
  "private": true,
  "description": "IrsanAI Live Share — local-first, session-driven live sharing. Domain scaffold + minimal HTTP server for real-device testing. v0.5.3 verifiziert: Build grün, Demo + HTTP live, 2030 Scaffold experimental.",
  "scripts": {
    "build": "tsc -p .",
    "demo": "npm run build && node dist/demo.js",
    "start": "npm run build && node dist/interfaces/http/server.js",
    "live": "npm run build && node dist/interfaces/http/server.js"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.10",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.7",
    "typescript": "^5.9.3"
  }
}
